# sin_bash.doc.md

`sin-bash` CLI shim — safe shell exec via `execute` Go binary..

## What this file does

`sin-bash` CLI shim — safe shell exec via `execute` Go binary.. See source file for implementation details.

## Dependencies

- See source file imports for dependency details.

## Usage

```bash
sin codocs check
```

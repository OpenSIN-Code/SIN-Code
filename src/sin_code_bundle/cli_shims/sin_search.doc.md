# sin_search.doc.md

`sin-search` CLI shim — wraps `scout` Go tool (regex/semantic/symbol)..

## What this file does

`sin-search` CLI shim — wraps `scout` Go tool (regex/semantic/symbol).. See source file for implementation details.

## Dependencies

- See source file imports for dependency details.

## Usage

```bash
sin codocs check
```

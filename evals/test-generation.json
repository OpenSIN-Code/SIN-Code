{
  "name": "Test Generation Golden Dataset",
  "version": "1.1.0",
  "description": "Evaluates the agent's ability to generate and run Go tests using sin_test_generate and sin_test (issue #75 / RFC-test-automation). Dual-mode contract: keyword cases stay green in offline stub mode; compile_and_run cases are gated behind --use-model (issue #264).",
  "test_cases": [
    {
      "id": "generate_test_for_add",
      "prompt": "There is a Go file calc.go with package calc and a function Add(a, b int) int. Use sin_test_generate to generate tests for it, then run sin_test to verify the tests pass.",
      "description": "Smoke test for sin_test_generate + sin_test workflow.",
      "tags": ["test-generation", "go", "stub-friendly"],
      "constraints": {
        "max_turns": 6,
        "require_verify": false,
        "timeout": "120s",
        "must_use_tools": ["sin_test_generate", "sin_test"]
      },
      "expected": {
        "output_contains": ["sin_test_generate", "sin_test"],
        "min_quality": 0.8
      },
      "metadata": {
        "priority": "high",
        "category": "workflow"
      }
    },
    {
      "id": "generate_test_with_existing_file",
      "prompt": "A file reverse.go contains package reverse with func Reverse(s string) string. The test file reverse_test.go does not exist yet. Generate tests with sin_test_generate and run them.",
      "description": "Tests generation of a previously untested file.",
      "tags": ["test-generation", "go", "stub-friendly"],
      "constraints": {
        "max_turns": 6,
        "require_verify": false,
        "timeout": "120s",
        "must_use_tools": ["sin_test_generate", "sin_test"]
      },
      "expected": {
        "output_contains": ["sin_test_generate", "reverse_test"],
        "min_quality": 0.8
      },
      "metadata": {
        "priority": "high",
        "category": "workflow"
      }
    },
    {
      "id": "quality_gate_passes",
      "prompt": "Run the quality gate with sin_quality_gate on the current workspace and ensure it returns PASS.",
      "description": "Verifies the sin_quality_gate tool can run successfully.",
      "tags": ["quality-gate", "go", "stub-friendly"],
      "constraints": {
        "max_turns": 4,
        "require_verify": false,
        "timeout": "120s",
        "must_use_tools": ["sin_quality_gate"]
      },
      "expected": {
        "output_contains": ["sin_quality_gate", "PASS"],
        "min_quality": 0.8
      },
      "metadata": {
        "priority": "medium",
        "category": "tool"
      }
    },
    {
      "id": "compile_fizzbuzz_self_check",
      "prompt": "Write a Go function fizzbuzz(n int) string that returns \"FizzBuzz\" when n is divisible by both 3 and 5, \"Fizz\" by 3, \"Buzz\" by 5, else the digit as string. Reply with the implementation in a fenced ```go``` code block.",
      "description": "Dual-mode: produces a code block the LLM-mode scorer can compile and self-check.",
      "tags": ["compile_and_run", "go", "model-only"],
      "constraints": {
        "max_turns": 4,
        "timeout": "60s"
      },
      "expected": {
        "output_contains": ["fizzbuzz", "```go"]
      },
      "scorer": {
        "type": "compile_and_run",
        "language": "go",
        "self_check": "if got := fizzbuzz(15); got != \"FizzBuzz\" { panic(\"15=\" + got) }\nif got := fizzbuzz(9); got != \"Fizz\" { panic(\"9=\" + got) }\nif got := fizzbuzz(10); got != \"Buzz\" { panic(\"10=\" + got) }\nif got := fizzbuzz(7); got != \"7\" { panic(\"7=\" + got) }",
        "timeout": "20s",
        "requires_model": true
      },
      "metadata": {
        "priority": "high",
        "category": "code-generation"
      }
    }
  ]
}

# Standards

- Follow the canonical SIN-Code skill lifecycle.
- Prefer the external canonical implementation until a native `sin-code supabase` command exists.
- Never expose credentials in logs or output.

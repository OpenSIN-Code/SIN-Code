# Standards

- Follow the canonical SIN-Code skill lifecycle.
- Prefer the external canonical `opencode-plan` implementation until a native `sin-code enterprise-plan` command exists.
- Always validate plans before execution.

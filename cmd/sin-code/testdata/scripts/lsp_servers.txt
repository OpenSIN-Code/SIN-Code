# Server detection must work on any machine: it reports either the detected
# servers or a friendly "none found" hint. Both are valid outcomes — the
# command must never fail just because no language server is installed.
env HOME=$WORK/home
mkdir $WORK/home
sin-code lsp servers
stdout '(Detected|no LSP servers detected)'
